// @generated by ccc from concept game.player (key c761929e78f6). Do not edit.
import { Hand } from './player/hand.js';
import type { UserId } from '../user.js';

export class Player {
  readonly user: UserId;
  readonly hand: Hand;
  private _points: number;

  constructor(user: UserId, hand?: Hand, points?: number) {
    this.user = user;
    this.hand = hand ?? new Hand();
    this._points = points ?? 0;
  }

  points(): number {
    return this._points;
  }

  scorePoint(): void {
    this._points++;
  }
}

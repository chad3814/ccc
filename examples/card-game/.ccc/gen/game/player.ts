// @generated by ccc from concept game.player (key 189767a8bf02). Do not edit.
import { Hand } from './player/hand.js';
import type { UserId } from '../user.js';

export class Player {
  readonly user: UserId;
  readonly hand: Hand;
  #points: number;

  constructor(user: UserId, hand: Hand = new Hand(), points = 0) {
    this.user = user;
    this.hand = hand;
    this.#points = points;
  }

  points(): number {
    return this.#points;
  }

  scorePoint(): void {
    this.#points += 1;
  }
}

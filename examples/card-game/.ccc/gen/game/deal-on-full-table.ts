// @generated by ccc from concept game.deal-on-full-table (key d77b2473bfc9). Do not edit.
import { Game } from '../game.js';
import { Players } from './players.js';

export interface SyncEvent {
  readonly target: Players;
  readonly args: Parameters<Players['join']>;
  readonly result: Awaited<ReturnType<Players['join']>>;
}

export interface SyncTargets {
  readonly game: Game;
}

export async function handle(event: SyncEvent, targets: SyncTargets): Promise<void> {
  // Deal only when the join left no seats and the game is still "waiting"
  if (event.target.seatsLeft() === 0 && targets.game.status() === 'waiting') {
    targets.game.deal();
  }
}

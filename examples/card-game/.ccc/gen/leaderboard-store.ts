// @generated by ccc from concept leaderboard-store (key e3e202ea79ed). Do not edit.
import type { Database } from '@ccc/runtime';
import type { Standing } from './leaderboard.js';
import { userId } from './user.js';

type StandingRow = {
  user_id: string;
  wins: number | string;
};

function toRows<T>(result: unknown): readonly T[] {
  if (Array.isArray(result)) return result as readonly T[];
  if (result !== null && typeof result === 'object' && 'rows' in result) {
    return (result as { rows: readonly T[] }).rows;
  }
  return [];
}

export class LeaderboardStore {
  private readonly db: Database;

  constructor(db: Database) {
    this.db = db;
  }

  async recordWin(user: string & { readonly __brand: 'UserId' }): Promise<void> {
    await this.db.query(
      `insert into leaderboard (user_id, wins) values ($1, 1)
       on conflict (user_id) do update set wins = leaderboard.wins + 1`,
      [user],
    );
  }

  async top(limit: number): Promise<Standing[]> {
    const result = await this.db.query(
      `select user_id, wins from leaderboard
       order by wins desc, user_id asc
       limit $1`,
      [limit],
    );
    return toRows<StandingRow>(result).map((row) => ({
      userId: userId(row.user_id),
      wins: Number(row.wins),
    }));
  }
}

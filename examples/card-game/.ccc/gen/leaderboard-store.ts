// @generated by ccc from concept leaderboard-store (key 299d308733cb). Do not edit.
import type { Database } from '@ccc/runtime';
import type { Standing } from './leaderboard.js';
import type { UserId } from './user.js';
import { userId } from './user.js';

type LeaderboardRow = {
  readonly user_id: string;
  readonly wins: number;
};

type QueryResult = {
  readonly rows: unknown[];
};

export class LeaderboardStore {
  private db: Database;

  constructor(db: Database) {
    this.db = db;
  }

  async recordWin(user: UserId): Promise<void> {
    await this.db.query(
      `INSERT INTO leaderboard (user_id, wins) VALUES ($1, 1)
       ON CONFLICT (user_id) DO UPDATE SET wins = leaderboard.wins + 1`,
      [user]
    );
  }

  async top(limit: number): Promise<Standing[]> {
    const result = (await this.db.query(
      `SELECT leaderboard.user_id, leaderboard.wins FROM leaderboard 
       ORDER BY leaderboard.wins DESC, leaderboard.user_id ASC 
       LIMIT $1`,
      [limit]
    )) as QueryResult;
    
    return (result.rows as LeaderboardRow[]).map((row) => ({
      userId: userId(row.user_id),
      wins: row.wins,
    }));
  }
}
